<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.dux.bbms2">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.ACTION_DIAL" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".bank_user.BankUpdateDetails"></activity>
        <activity android:name=".individual_user.SearchIndividualUser" />
        <activity android:name=".individual_user.UpdateDetails" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ForgotPassword"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Dashboard" />
        <activity android:name=".UpdateProfile" />
        <activity android:name=".bank_user.BloodBankDashboard" />
        <activity android:name=".individual_user.IndividualUserDashboard" />
        <activity android:name=".bank_user.BloodBankRegister" />
        <activity android:name=".individual_user.IndividualRegister" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".individual_user.SearchBloodBank"
            android:label="Search Blood Bank" />
        <activity
            android:name=".bank_user.UpdateBlood"
            android:label="Update Blood" />
        <activity android:name=".ChangePassword" />
    </application>

</manifest>